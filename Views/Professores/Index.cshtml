@model IEnumerable<AprendaMaisSistema.Models.Professores>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeProfessores)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeProfessores)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfessoresId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProfessoresId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProfessoresId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
