@model AprendaMaisSistema.Models.Materias

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Materias</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeMaterias)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeMaterias)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.MateriasId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
